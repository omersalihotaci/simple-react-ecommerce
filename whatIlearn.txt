1-async fonksiyon her zaman promise döner.

async function hello() {
  return "Merhaba";
}
Sen sadece "Merhaba" return ettin.

JavaScript bunu otomatik olarak şöyle sarar:
return Promise.resolve("Merhaba");

2-flat iç içe dizileri tek bir diziye çevirir.

3-Promise.all(array) promisleri toplar tek bir promise döner.

4-await promisleri beklemek ve açmak için kullanılır.kod os satırda fulfileld veya rejected olana kadar bekler  daha sonra await sayesinde yine promisin içindeki değeri alabiliriz.

5-props geçince typescripte türünü verirken iki yolun var:

A-props olarak al ve propsun türünü yukarda tanımlayarak ona bu türü ver.
B-Inline destructuring { name }: { name: string }

6-usenavigate() butonlarda kullanılır hooktur.Navigate componenti koşullu yönlendirmelerde routes işlemlerini yaparken kullanılır.

7-parent element render edilirse childda render edilir fakat aksi mümkün deil.

8-inputa name vermek zorudnasın ve name isimleri ile initial valuesdeki isimler aynı olmalı ki eşleşsinler

formik bize nesne döner yönetmemiz için formu ve bizde ona parametre olarak en az 2 değer vermeliyiz.onsubmit ve initialvalues.

 text alanına girilen değerler formikin içindeki valueste tutulur.


 formik
│
├── values        -> { username, password }
├── errors        -> { username, password }
├── touched       -> { username, password }
├── handleChange  -> function
├── handleBlur    -> function
├── handleSubmit  -> function
├── setFieldValue -> function
└── resetForm     -> function

9-spread yaparken array açılır array içinde array gibi bir durum söz konusu deil.
 
 
 
 
 
 
 formik kullanmasan tek tek usestate kullanıp her bşrini ayrı ayrı functionla değişticiektin bu derli toplu görğntü veriyor bide yup kullanman için



 validationSchema ismi zorunlu buna da yupunu bağlarsın

 onblur özelliği kullanıcı inputa tıklayı ardından başka bir yere tıklayınca çalışır.
 formik.handleBlur → Formik’e “bu input’a dokunuldu ve focus kayboldu” bilgisini bildirir

Formik bunu touched nesnesinde saklar:
formik.touched.username // true veya false
